name: Build ROCm Wheels


on:
  workflow_dispatch:
    inputs:
      python-version:
        description: "Python version"
        required: true
        type: string
        default: "3.12"
      rocm-versions:
        description: "ROCm versions to build (JSON array). Must have torch wheels for torch-version (7.0→2.10 only; 7.1/7.2→2.11+)."
        required: true
        type: string
        default: '["7.2"]'
      torch-version:
        description: "PyTorch version (e.g., 2.12.0)"
        required: true
        type: string
        default: "2.12.0"
      pytorch-rocm-arch:
        description: "ROCm GPU architectures to target (semicolon-separated)"
        required: true
        type: string
        default: "gfx950"

      runs-on-long:
        description: "Base labels for long (>6h) self-hosted builds; X64 is appended (ROCm is x86_64-only)"
        required: true
        type: string
        default: '["self-hosted","linux","cpu"]'
      container-image:
        description: "Container image for long builds (isolates apt/pip from the host)"
        required: true
        type: string
        default: "ubuntu:24.04"
      max-jobs-long:
        description: "Compile parallelism for long/self-hosted builds (0 = nproc)"
        required: true
        type: string
        default: "0"

      upload-to-release:
        description: "Upload built wheels to a GitHub release"
        type: boolean
        default: true
      override-previous:
        description: "Override existing release assets with the same name"
        type: boolean
        default: false

defaults:
  run:
    shell: bash -x -e -u -o pipefail {0}

jobs:
  create-release:
    name: Create release (ROCm ${{ matrix.rocm-version }})
    if: ${{ inputs.upload-to-release }}
    timeout-minutes: 2160
    runs-on: ubuntu-latest
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    steps:
      - uses: actions/checkout@v5

      - name: Create release (if it does not exist)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          TAG="torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}"
          gh release create "$TAG" \
            --title "$TAG" \
            --notes "ROCm wheels for PyTorch ${{ inputs.torch-version }} + ROCm ${{ matrix.rocm-version }}" \
            2>/dev/null || true

  build-xformers:
    needs: [create-release]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    runs-on: ${{ fromJSON(format('["{0}","{1}"]', join(fromJSON(inputs.runs-on-long), '","'), 'X64')) }}
    container:
      image: ${{ inputs.container-image }}
    env:
      WHEELS_IN_CONTAINER: "1"
      MAX_JOBS: ${{ inputs.max-jobs-long }}
    name: Build xformers (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Bootstrap container
        if: ${{ env.WHEELS_IN_CONTAINER == '1' }}
        run: bash scripts/ci-bootstrap-container.sh

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARCH=$(uname -m)
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep "^xformers-" | grep -q "$ARCH"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::xformers ($ARCH) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Free disk space
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Set up swap space
        if: ${{ steps.check.outputs.skip != 'true' && env.WHEELS_IN_CONTAINER != '1' }}
        uses: pierotofy/set-swap-space@v1.0
        with:
          swap-size-gb: 16

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          echo "PYTORCH_ROCM_ARCH=${{ inputs.pytorch-rocm-arch }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}"

      - name: Install PyTorch (ROCm)
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir numpy
          pip install --no-cache-dir torch==${{ inputs.torch-version }} \
            --index-url https://download.pytorch.org/whl/${ROCM_SHORT}

      - name: Build xformers
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-xformers.sh

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          gh release upload "${{ steps.rel.outputs.tag }}" wheels/*.whl $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
            if grep -q "already exists" /tmp/upload.log; then
              echo "::warning::Wheel already exists in release, skipping upload"
            else
              exit 1
            fi
          }

  build-pyg:
    needs: [create-release]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
        package: [pytorch_cluster, pytorch_sparse, pytorch_scatter]
    runs-on: ubuntu-24.04
    name: Build ${{ matrix.package }} (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARCH=$(uname -m)
          WHEEL_PREFIX=$(echo "${{ matrix.package }}" | sed 's/^pytorch_/torch_/')
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep "^${WHEEL_PREFIX}-" | grep -q "$ARCH"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::${WHEEL_PREFIX} ($ARCH) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Free disk space
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Set up swap space
        if: ${{ steps.check.outputs.skip != 'true' && env.WHEELS_IN_CONTAINER != '1' }}
        uses: pierotofy/set-swap-space@v1.0
        with:
          swap-size-gb: 16

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          echo "PYTORCH_ROCM_ARCH=${{ inputs.pytorch-rocm-arch }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}"

      - name: Install PyTorch (ROCm)
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir numpy
          pip install --no-cache-dir torch==${{ inputs.torch-version }} \
            --index-url https://download.pytorch.org/whl/${ROCM_SHORT}

      - name: Build ${{ matrix.package }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-pyg.sh "${{ matrix.package }}"

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          gh release upload "${{ steps.rel.outputs.tag }}" wheels/*.whl $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
            if grep -q "already exists" /tmp/upload.log; then
              echo "::warning::Wheel already exists in release, skipping upload"
            else
              exit 1
            fi
          }

  build-torchao:
    needs: [create-release]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    runs-on: ubuntu-24.04
    name: Build torchao (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARCH=$(uname -m)
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep "^torchao-" | grep -q "$ARCH"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::torchao ($ARCH) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Free disk space
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Set up swap space
        if: ${{ steps.check.outputs.skip != 'true' && env.WHEELS_IN_CONTAINER != '1' }}
        uses: pierotofy/set-swap-space@v1.0
        with:
          swap-size-gb: 16

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          echo "PYTORCH_ROCM_ARCH=${{ inputs.pytorch-rocm-arch }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}"

      - name: Install PyTorch (ROCm)
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir numpy
          pip install --no-cache-dir torch==${{ inputs.torch-version }} \
            --index-url https://download.pytorch.org/whl/${ROCM_SHORT}

      - name: Build torchao
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-torchao.sh

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          gh release upload "${{ steps.rel.outputs.tag }}" wheels/*.whl $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
            if grep -q "already exists" /tmp/upload.log; then
              echo "::warning::Wheel already exists in release, skipping upload"
            else
              exit 1
            fi
          }

  build-flash-attention:
    needs: [create-release]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    runs-on: ${{ fromJSON(format('["{0}","{1}"]', join(fromJSON(inputs.runs-on-long), '","'), 'X64')) }}
    container:
      image: ${{ inputs.container-image }}
    env:
      WHEELS_IN_CONTAINER: "1"
      MAX_JOBS: ${{ inputs.max-jobs-long }}
    name: Build flash-attention (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Bootstrap container
        if: ${{ env.WHEELS_IN_CONTAINER == '1' }}
        run: bash scripts/ci-bootstrap-container.sh

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARCH=$(uname -m)
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep "^flash_attn-" | grep -v "fa4" | grep -q "$ARCH"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::flash-attention 2 ($ARCH) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Free disk space
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Set up swap space
        if: ${{ steps.check.outputs.skip != 'true' && env.WHEELS_IN_CONTAINER != '1' }}
        uses: pierotofy/set-swap-space@v1.0
        with:
          swap-size-gb: 16

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          echo "PYTORCH_ROCM_ARCH=${{ inputs.pytorch-rocm-arch }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}"

      - name: Install PyTorch (ROCm)
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir numpy
          pip install --no-cache-dir torch==${{ inputs.torch-version }} \
            --index-url https://download.pytorch.org/whl/${ROCM_SHORT}

      - name: Build flash-attention
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-flash-attention.sh

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          gh release upload "${{ steps.rel.outputs.tag }}" wheels/*.whl $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
            if grep -q "already exists" /tmp/upload.log; then
              echo "::warning::Wheel already exists in release, skipping upload"
            else
              exit 1
            fi
          }

  build-aiter:
    needs: [create-release]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    runs-on: ${{ fromJSON(format('["{0}","{1}"]', join(fromJSON(inputs.runs-on-long), '","'), 'X64')) }}
    container:
      image: ${{ inputs.container-image }}
    env:
      WHEELS_IN_CONTAINER: "1"
      MAX_JOBS: ${{ inputs.max-jobs-long }}
    name: Build aiter (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Bootstrap container
        if: ${{ env.WHEELS_IN_CONTAINER == '1' }}
        run: bash scripts/ci-bootstrap-container.sh

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARCH=$(uname -m)
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep "^aiter-" | grep -q "$ARCH"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::aiter ($ARCH) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Free disk space
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Set up swap space
        if: ${{ steps.check.outputs.skip != 'true' && env.WHEELS_IN_CONTAINER != '1' }}
        uses: pierotofy/set-swap-space@v1.0
        with:
          swap-size-gb: 16

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          echo "PYTORCH_ROCM_ARCH=${{ inputs.pytorch-rocm-arch }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}"

      - name: Install PyTorch (ROCm)
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir numpy
          pip install --no-cache-dir torch==${{ inputs.torch-version }} \
            --index-url https://download.pytorch.org/whl/${ROCM_SHORT}

      - name: Build aiter
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-aiter.sh

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          gh release upload "${{ steps.rel.outputs.tag }}" wheels/*.whl $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
            if grep -q "already exists" /tmp/upload.log; then
              echo "::warning::Wheel already exists in release, skipping upload"
            else
              exit 1
            fi
          }

  build-amdsmi:
    needs: [create-release]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    runs-on: ubuntu-24.04
    name: Build amdsmi (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # amdsmi is pure Python (py3-none-any); do not require $ARCH in the name.
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep -qE '^amdsmi-.*-py3-none-any\.whl$'; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::amdsmi (py3-none-any) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}" --with-amdsmi

      - name: Build amdsmi
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-amdsmi.sh

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          gh release upload "${{ steps.rel.outputs.tag }}" wheels/*.whl $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
            if grep -q "already exists" /tmp/upload.log; then
              echo "::warning::Wheel already exists in release, skipping upload"
            else
              exit 1
            fi
          }

  build-vllm:
    needs: [create-release, build-flash-attention, build-aiter, build-amdsmi]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    runs-on: ${{ fromJSON(format('["{0}","{1}"]', join(fromJSON(inputs.runs-on-long), '","'), 'X64')) }}
    container:
      image: ${{ inputs.container-image }}
    env:
      WHEELS_IN_CONTAINER: "1"
      MAX_JOBS: ${{ inputs.max-jobs-long }}
    name: Build vllm (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Bootstrap container
        if: ${{ env.WHEELS_IN_CONTAINER == '1' }}
        run: bash scripts/ci-bootstrap-container.sh

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARCH=$(uname -m)
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep "^vllm-" | grep -q "$ARCH"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::vllm ($ARCH) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Free disk space
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Set up swap space
        if: ${{ steps.check.outputs.skip != 'true' && env.WHEELS_IN_CONTAINER != '1' }}
        uses: pierotofy/set-swap-space@v1.0
        with:
          swap-size-gb: 16

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          echo "PYTORCH_ROCM_ARCH=${{ inputs.pytorch-rocm-arch }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}" --with-amdsmi

      - name: Install PyTorch (ROCm)
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir numpy
          pip install --no-cache-dir torch==${{ inputs.torch-version }} \
            --index-url https://download.pytorch.org/whl/${ROCM_SHORT}

      - name: Download dependency wheels from release
        if: ${{ steps.check.outputs.skip != 'true' }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          mkdir -p wheels
          for prefix in flash_attn aiter amdsmi; do
            ASSET=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q ".assets[].name" 2>/dev/null \
              | grep "^${prefix}-" | head -1 || true)
            if [ -n "$ASSET" ]; then
              echo "==> Downloading $ASSET"
              gh release download "${{ steps.rel.outputs.tag }}" -p "$ASSET" -D wheels/
            else
              echo "::warning::No ${prefix} wheel found in release ${{ steps.rel.outputs.tag }}"
            fi
          done

      - name: Build vllm
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-vllm.sh

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          for whl in wheels/vllm-*.whl; do
            [ -f "$whl" ] || continue
            gh release upload "${{ steps.rel.outputs.tag }}" "$whl" $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
              if grep -q "already exists" /tmp/upload.log; then
                echo "::warning::Wheel already exists in release, skipping upload"
              else
                exit 1
              fi
            }
          done

  build-mslk:
    needs: [create-release]
    if: ${{ always() && !cancelled() }}
    timeout-minutes: 2160
    permissions:
      contents: write
    strategy:
      fail-fast: false
      matrix:
        rocm-version: ${{ fromJSON(inputs.rocm-versions) }}
    runs-on: ${{ fromJSON(format('["{0}","{1}"]', join(fromJSON(inputs.runs-on-long), '","'), 'X64')) }}
    container:
      image: ${{ inputs.container-image }}
    env:
      WHEELS_IN_CONTAINER: "1"
      MAX_JOBS: ${{ inputs.max-jobs-long }}
    name: Build mslk (ROCm ${{ matrix.rocm-version }})
    steps:
      - uses: actions/checkout@v5

      - name: Bootstrap container
        if: ${{ env.WHEELS_IN_CONTAINER == '1' }}
        run: bash scripts/ci-bootstrap-container.sh

      - name: Compute release tag
        id: rel
        run: |
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          TORCH_MAJOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f1)
          TORCH_MINOR=$(echo "${{ inputs.torch-version }}" | cut -d. -f2)
          echo "tag=torch${TORCH_MAJOR}.${TORCH_MINOR}-rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_OUTPUT

      - name: Check if wheel already exists
        id: check
        if: ${{ inputs.upload-to-release && !inputs.override-previous }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          ARCH=$(uname -m)
          ASSETS=$(gh release view "${{ steps.rel.outputs.tag }}" --json assets -q '.assets[].name' 2>/dev/null || echo "")
          if echo "$ASSETS" | grep "^mslk-" | grep -q "$ARCH"; then
            echo "skip=true" >> $GITHUB_OUTPUT
            echo "::notice::mslk ($ARCH) wheel already exists in release ${{ steps.rel.outputs.tag }}, skipping build"
          fi

      - name: Free disk space
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: sudo rm -rf /usr/share/dotnet /opt/ghc /opt/hostedtoolcache/CodeQL

      - name: Set up swap space
        if: ${{ steps.check.outputs.skip != 'true' && env.WHEELS_IN_CONTAINER != '1' }}
        uses: pierotofy/set-swap-space@v1.0
        with:
          swap-size-gb: 16

      - name: Set up Python
        if: ${{ steps.check.outputs.skip != 'true' }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ inputs.python-version }}

      - name: Set environment from inputs
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          echo "GPU_BACKEND=rocm" >> $GITHUB_ENV
          echo "PYTORCH_VERSION=${{ inputs.torch-version }}" >> $GITHUB_ENV
          echo "ROCM_VERSION=${{ matrix.rocm-version }}" >> $GITHUB_ENV
          echo "PYTORCH_ROCM_ARCH=${{ inputs.pytorch-rocm-arch }}" >> $GITHUB_ENV
          ROCM_MAJOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f1)
          ROCM_MINOR=$(echo "${{ matrix.rocm-version }}" | cut -d. -f2)
          echo "ROCM_SHORT=rocm${ROCM_MAJOR}.${ROCM_MINOR}" >> $GITHUB_ENV

      - name: Install ROCm ${{ matrix.rocm-version }}
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/ci-install-rocm.sh "${{ matrix.rocm-version }}"

      - name: Install PyTorch (ROCm)
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: |
          pip install --upgrade pip
          pip install --no-cache-dir numpy
          pip install --no-cache-dir torch==${{ inputs.torch-version }} \
            --index-url https://download.pytorch.org/whl/${ROCM_SHORT}

      - name: Build mslk
        if: ${{ steps.check.outputs.skip != 'true' }}
        run: bash scripts/build-mslk.sh

      - name: Upload wheel to release
        if: ${{ steps.check.outputs.skip != 'true' && inputs.upload-to-release }}
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          CLOBBER_FLAG=""
          if [ "${{ inputs.override-previous }}" = "true" ]; then
            CLOBBER_FLAG="--clobber"
          fi
          for whl in wheels/mslk-*.whl; do
            [ -f "$whl" ] || continue
            gh release upload "${{ steps.rel.outputs.tag }}" "$whl" $CLOBBER_FLAG 2>&1 | tee /tmp/upload.log || {
              if grep -q "already exists" /tmp/upload.log; then
                echo "::warning::Wheel already exists in release, skipping upload"
              else
                exit 1
              fi
            }
          done
